# Git 版本回退完全指南

> 适用于微信小程序项目的 Git 版本管理教程

---

## 目录
1. [基础概念](#基础概念)
2. [查看提交历史](#查看提交历史)
3. [三种回退方法](#三种回退方法)
4. [实战案例](#实战案例)
5. [常见问题](#常见问题)
6. [最佳实践](#最佳实践)

---

## 基础概念

### Git 的三个区域
```
工作区 (Working Directory)  →  暂存区 (Staging Area)  →  版本库 (Repository)
   ↓                              ↓                          ↓
 你编辑的文件                   git add 后的文件          git commit 后的版本
```

### 重要术语
- **HEAD**: 当前所在的版本（指针）
- **commit**: 一次提交记录（快照）
- **hash**: 提交的唯一标识符（如 `6df3b50`）
- **origin**: 远程仓库（GitHub）

---

## 查看提交历史

### 1. 查看简洁历史（推荐）
```bash
git log --oneline -10
```
**输出示例**：
```
6df3b50 feat: 恢复民族脊梁功能模块
ecfea82 fix: 修复assets.js中Base64字符串截断
82bad11 fix: 创建hero-detail占位页面
c2dff73 fix: 修复编译错误和游客模式限制
0c7269f feat: 数据层重构 - 构建Service层
b1aa44e feat: 资源灾难修复 - 添加SVG图标库
43324be refactor: 移除民族脊梁功能模块  ← 这是删除的提交
```

### 2. 查看详细历史
```bash
git log --oneline --graph --all -20
```
显示分支图和最近 20 条记录。

### 3. 查看某个文件的历史
```bash
git log --oneline -- miniprogram/data/heroes.js
```

### 4. 查看某次提交的详细内容
```bash
git show 43324be
```

---

## 三种回退方法

### 方法 1: `git revert` - 撤销某次提交（推荐）

**特点**：
- ✅ 安全：不会删除历史记录
- ✅ 适合团队协作
- ✅ 可以撤销中间的某次提交
- ❌ 会产生新的提交记录

**使用场景**：已经推送到远程仓库，需要撤销某次错误提交。

#### 示例：撤销"移除民族脊梁"的提交
```bash
# 1. 查找要撤销的提交 hash
git log --oneline | grep "移除民族脊梁"
# 输出: 43324be refactor: 移除民族脊梁功能模块

# 2. 撤销该提交（会自动创建一个新的反向提交）
git revert 43324be

# 3. 如果有冲突，手动解决后：
git add .
git revert --continue

# 4. 推送到远程
git push origin main
```

**原理**：
```
之前: A → B → C(删除) → D → E
之后: A → B → C(删除) → D → E → F(恢复)
```

---

### 方法 2: `git reset` - 回退到指定版本（危险）

**特点**：
- ⚠️ 危险：会删除历史记录
- ⚠️ 不适合团队协作
- ✅ 可以完全回到过去
- ✅ 有三种模式可选

**使用场景**：本地开发，还未推送到远程，想完全撤销最近的提交。

#### 三种模式对比

| 模式 | 工作区 | 暂存区 | 版本库 | 使用场景 |
|------|--------|--------|--------|----------|
| `--soft` | 保留 | 保留 | 回退 | 想重新提交 |
| `--mixed` (默认) | 保留 | 清空 | 回退 | 想重新 add |
| `--hard` | 清空 | 清空 | 回退 | 完全放弃修改 |

#### 示例 1: 回退到删除民族脊梁之前
```bash
# 1. 查找删除之前的提交
git log --oneline
# 假设删除之前的提交是 a1b2c3d

# 2. 回退到该版本（保留工作区修改）
git reset --soft a1b2c3d

# 3. 或者完全回退（丢弃所有修改）
git reset --hard a1b2c3d

# 4. 强制推送到远程（危险！）
git push origin main --force
```

#### 示例 2: 撤销最近 3 次提交
```bash
# 回退 3 个版本，保留修改
git reset --soft HEAD~3

# 回退 3 个版本，丢弃修改
git reset --hard HEAD~3
```

**原理**：
```
之前: A → B → C(删除) → D → E
之后: A → B  (C、D、E 被删除)
```

---

### 方法 3: `git checkout` - 恢复单个文件

**特点**：
- ✅ 精确：只恢复特定文件
- ✅ 安全：不影响其他文件
- ✅ 灵活：可以从任意版本恢复

**使用场景**：只想恢复某个文件到之前的版本。

#### 示例：恢复 heroes.js 文件
```bash
# 1. 查找包含该文件的提交
git log --oneline -- miniprogram/data/heroes.js

# 2. 从指定版本恢复该文件
git checkout 43324be -- miniprogram/data/heroes.js

# 3. 提交恢复的文件
git add miniprogram/data/heroes.js
git commit -m "恢复英雄数据文件"
git push origin main
```

---

## 实战案例

### 案例 1: 恢复"民族脊梁"功能（已推送到远程）

**场景**：你在 `43324be` 提交中删除了民族脊梁功能，现在想恢复。

**步骤**：
```bash
# 1. 查看提交历史
git log --oneline -10

# 2. 使用 revert 撤销删除操作
git revert 43324be

# 3. 编辑器会打开，输入提交信息：
# "revert: 恢复民族脊梁功能模块"

# 4. 如果有冲突，解决后：
git add .
git revert --continue

# 5. 推送到远程
git push origin main
```

---

### 案例 2: 回退到昨天的版本（未推送）

**场景**：今天的修改全错了，想回到昨天。

**步骤**：
```bash
# 1. 查看昨天的提交
git log --oneline --since="yesterday"

# 2. 假设昨天最后一次提交是 abc123d
git reset --hard abc123d

# 3. 如果已经推送，需要强制推送（危险！）
git push origin main --force
```

---

### 案例 3: 只恢复某个文件夹

**场景**：只想恢复 `pages/hero-gallery/` 文件夹。

**步骤**：
```bash
# 1. 查找包含该文件夹的提交
git log --oneline -- miniprogram/pages/hero-gallery/

# 2. 从指定版本恢复整个文件夹
git checkout 43324be -- miniprogram/pages/hero-gallery/

# 3. 提交
git add miniprogram/pages/hero-gallery/
git commit -m "恢复英雄长廊页面"
git push origin main
```

---

### 案例 4: 撤销最近一次提交（未推送）

**场景**：刚提交完发现写错了，还没推送。

**步骤**：
```bash
# 方法 1: 撤销提交，保留修改（可以重新提交）
git reset --soft HEAD~1

# 方法 2: 撤销提交和 add，保留文件修改
git reset HEAD~1

# 方法 3: 完全撤销，丢弃所有修改
git reset --hard HEAD~1
```

---

### 案例 5: 修改最近一次提交信息

**场景**：提交信息写错了。

**步骤**：
```bash
# 如果还没推送
git commit --amend -m "新的提交信息"

# 如果已经推送
git commit --amend -m "新的提交信息"
git push origin main --force
```

---

## 常见问题

### Q1: `git reset --hard` 后能恢复吗？
**A**: 可以！使用 `git reflog` 查找丢失的提交。

```bash
# 1. 查看所有操作记录
git reflog

# 2. 找到想恢复的提交 hash
# 输出: abc123d HEAD@{5}: reset: moving to HEAD~3

# 3. 恢复到该提交
git reset --hard abc123d
```

---

### Q2: 如何撤销已推送的提交？
**A**: 使用 `git revert`（推荐）或 `git reset + --force`（危险）。

```bash
# 方法 1: 安全的方式
git revert <commit-hash>
git push origin main

# 方法 2: 危险的方式（会影响其他人）
git reset --hard <commit-hash>
git push origin main --force
```

---

### Q3: 如何查看某次提交修改了哪些文件？
**A**: 使用 `git show` 或 `git diff`。

```bash
# 查看某次提交的详细内容
git show 43324be

# 查看某次提交修改的文件列表
git show --name-only 43324be

# 对比两次提交的差异
git diff 43324be 6df3b50
```

---

### Q4: 如何恢复已删除的文件？
**A**: 使用 `git checkout` 或 `git restore`。

```bash
# 方法 1: 从最近的提交恢复
git checkout HEAD -- miniprogram/data/heroes.js

# 方法 2: 从指定提交恢复
git checkout 43324be -- miniprogram/data/heroes.js

# 方法 3: 使用新命令（Git 2.23+）
git restore --source=43324be miniprogram/data/heroes.js
```

---

### Q5: 如何撤销 `git add`？
**A**: 使用 `git reset` 或 `git restore`。

```bash
# 撤销所有 add
git reset

# 撤销单个文件的 add
git reset miniprogram/data/heroes.js

# 使用新命令（Git 2.23+）
git restore --staged miniprogram/data/heroes.js
```

---

## 最佳实践

### 1. 提交前的检查清单
```bash
# ✅ 查看修改了哪些文件
git status

# ✅ 查看具体修改内容
git diff

# ✅ 只提交需要的文件
git add <specific-files>

# ✅ 写清晰的提交信息
git commit -m "feat: 添加XXX功能"
```

---

### 2. 提交信息规范
```
<type>: <subject>

<body>

<footer>
```

**type 类型**：
- `feat`: 新功能
- `fix`: 修复 bug
- `refactor`: 重构代码
- `docs`: 文档更新
- `style`: 代码格式调整
- `test`: 测试相关
- `chore`: 构建/工具相关

**示例**：
```bash
git commit -m "feat: 添加民族脊梁功能模块

- 新增英雄数据文件
- 创建英雄长廊页面
- 创建英雄详情页面
- 集成图片加载工具

Closes #123"
```

---

### 3. 分支管理策略

```bash
# 创建功能分支
git checkout -b feature/hero-gallery

# 开发完成后合并到主分支
git checkout main
git merge feature/hero-gallery

# 删除功能分支
git branch -d feature/hero-gallery
```

---

### 4. 避免强制推送

```bash
# ❌ 危险：会覆盖远程仓库
git push origin main --force

# ✅ 安全：如果远程有新提交会失败
git push origin main --force-with-lease
```

---

### 5. 定期备份

```bash
# 创建备份分支
git branch backup-20251122

# 推送备份到远程
git push origin backup-20251122

# 需要时恢复
git checkout backup-20251122
```

---

## 快速参考表

| 操作 | 命令 | 说明 |
|------|------|------|
| 查看历史 | `git log --oneline -10` | 查看最近 10 条提交 |
| 撤销提交 | `git revert <hash>` | 安全撤销（推荐） |
| 回退版本 | `git reset --hard <hash>` | 危险回退 |
| 恢复文件 | `git checkout <hash> -- <file>` | 恢复单个文件 |
| 查看差异 | `git diff <hash1> <hash2>` | 对比两个版本 |
| 撤销 add | `git reset <file>` | 取消暂存 |
| 修改提交 | `git commit --amend` | 修改最近一次提交 |
| 查看操作记录 | `git reflog` | 查找丢失的提交 |

---

## 紧急救援

### 场景 1: 误删了重要文件
```bash
git reflog
git checkout <hash> -- <file>
```

### 场景 2: 提交了敏感信息
```bash
git reset --hard HEAD~1
git push origin main --force-with-lease
```

### 场景 3: 合并冲突无法解决
```bash
git merge --abort
# 或
git rebase --abort
```

---

## 总结

### 选择合适的回退方法

| 场景 | 推荐方法 | 命令 |
|------|----------|------|
| 已推送，需要撤销 | `git revert` | `git revert <hash>` |
| 未推送，想重新提交 | `git reset --soft` | `git reset --soft HEAD~1` |
| 未推送，完全放弃 | `git reset --hard` | `git reset --hard HEAD~1` |
| 只恢复某个文件 | `git checkout` | `git checkout <hash> -- <file>` |

### 记住这三条原则

1. **已推送的提交，用 `revert`**
2. **未推送的提交，用 `reset`**
3. **不确定时，先备份分支**

---

**祝你 Git 使用愉快！** 🎉

如有问题，随时查阅本文档或使用 `git --help` 命令。
